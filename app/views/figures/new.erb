<h1>Create a New Figure</h1>
<form action="/figures" method="post">
  <p>Name: <input type="text" name="figure[name]" id="figure_name"></p>
  <p>Title(s):
    <% Title.all.each do |t| %>
    <input type="checkbox" id="title_<%=t.id%>" name="figure[title_ids][]" value="<%= t.id %>"><%= t.name %>
    <% end %>
  </p>
  <p>Or create you own: <input type="text" id="new_title" name="title[name]"></p>
  <p>Landmark(s):
    <% Landmark.all.each do |l| %>
    <input type="checkbox" id="landmark_<%=l.id%>" name="figure[landmark_ids][]" value="<%= l.id %>"><%= l.name %>
    <% end %>
  </p>
  <p>Or create you own: <input type="text" id="new_landmark" name="landmark[name]"></p>
  <input type="submit" value="Create New Figure">
</form>
